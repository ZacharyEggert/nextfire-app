import type { NextPage } from 'next';
import Loader from '../components/Loader';

const Home: NextPage = () => {
    return (
            <main className='container'>
            </main>
    );
};

export default Home;
